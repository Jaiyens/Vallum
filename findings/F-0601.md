---
id: F-0601
agent: critic-voice
severity: P1
page: /
status: fixed
claim: Gallery's actual h2 is a 14-word run-on, not the 3 fragments shown
rule: VOICE.md Heading grammar (8-word hard cap, shapes A-D, C7/C3)
---

**String** (the section's only heading, `role="heading" aria-level={2}`, and exactly what `artifacts/1440/home.dom.txt` records as the level=2 heading):

"the work that kills, the work no one records, the work machines must learn"

**Location:** `src/content/gallery.ts` — `GALLERY_CENTERPIECE.ariaLabel`. Rendered by `components/gallery/Centerpiece.tsx` and `components/gallery/gallery-static.tsx`.

**The rule it breaks and the score:** Heading grammar caps headings at 8 words hard, and requires shape A/B/C/D (C7). Sighted users never read this string. What they see is a typewriter cycling three short lines — "the work that kills." / "the work no one records." / "the work machines must learn." — each one independently a clean Shape C fragment at 5-6 words. But the single string that is the actual page heading for every screen reader, crawler, and this very DOM capture is the concatenation: 14 words, comma-spliced, matching no heading shape, and its first five words ("the work that kills, the") contain no concrete noun (C3 also fails — "work" is the closest candidate and is abstract by the rubric's own photographable/countable test). Gates pass. Checks: 5 of 7 applicable (C1, C5, C9, C10, C12 pass; C3, C7 fail) = 71%, below the 83% pass threshold. This is precisely a case where the visual experience complies and the string a machine or assistive-tech user actually receives does not — the crawl caught it because it's a real accessible-name defect, not a rendering artifact.

**The rewrite:** the visual typewriter lines are already compliant and don't need to change. Only the flattened accessible name needs to shrink, and the third clause is redundant anyway — the very next heading on the page is "Machines are learning this work by watching," which already carries that idea. Trim `ariaLabel` to two fragments:

"The work that kills. The work no one records."

(8 words, Shape C, no meaning lost, no new claim added.)
