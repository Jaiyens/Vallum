---
id: F-0608
agent: critic-voice
severity: P1
page: /dataset
status: fixed
claim: Consent-and-provenance list wording differs between / and /dataset for two items
---

**Strings** (same schema, two pages, `DATASET_COPY.stack` in `src/content/sections.ts` versus `DATASET_PAGE.stack` in `src/content/dataset-page.ts`):

Home (`/`, `sections.ts` lines 45-46):
- "Site-owner authorization for every location"
- "Face blur applied before export"

Dataset (`/dataset`, `dataset-page.ts` lines 34-35):
- "Site-owner authorization"
- "Face blur applied before export. Non-negotiable"

**Location:** `src/content/sections.ts` (`DATASET_COPY.stack`, rendered by `components/sections/dataset.tsx`) and `src/content/dataset-page.ts` (`DATASET_PAGE.stack`, rendered by `app/dataset/page.tsx`).

**The rule it breaks and the score:** the task brief for this pass asks directly whether / and /dataset "sound like one writer." They should, and for this specific list they do not: the same six-item consent-and-provenance stack is authored twice as two independent strings and two of the six items diverge. Item 2 gains a clause on the home page ("for every location") that is not in FACTS.md's "Site-owner authorization" line and is not on /dataset. Item 3 gains a clause on /dataset ("Non-negotiable," which is in FACTS.md verbatim) that is dropped on the home page. This is the exact list VOICE.md holds up as the register's proof point, a Sotheby's-style itemized stack where "nothing else could be mistaken for it" (Banned Constructions, contrast to construction 1). A technical buyer doing diligence, the reader this site names as its audience, is the one reader most likely to open both pages and compare this exact stack line by line, and it will not match. Gates pass (neither variant contradicts FACTS.md on its own). Checks: this is a cross-page consistency failure the single-string rubric does not score, which is why it needs stating directly rather than as a per-string point count.

**The rewrite:** pick one wording per item and use it verbatim on both pages, matching FACTS.md's exact phrasing (FACTS.md: "Site-owner authorization" / "Face blur applied before export. Non-negotiable"):

"Site-owner authorization"
"Face blur applied before export. Non-negotiable"

Update `DATASET_COPY.stack` in `sections.ts` to these two lines so both pages render the identical six-item list.
