---
id: F-0410
agent: critic-eyes
severity: P1
page: /
viewport: 1440
status: wontfix
claim: Beat 4/5 section headings use bold grotesk black, not spec'd Newsreader forest
---

`LOOK.md`'s type table assigns every heading and display line to Newsreader (steps `display-1`, `display-2`, `heading` are all Newsreader 400/500); Inter is reserved for `body` and `small`. It also assigns beats 4 and 5's section lines specifically: "display-2, forest on bone." Zoomed crops of `artifacts/1440/home.png` show "Machines are learning this work by watching", "Consent first, camera second", and "What ships with every clip" all set in a bold grotesk sans (Inter Bold or equivalent, thick geometric strokes, no serifs) at near-black ink, not forest (`#16281D`). This is confirmed against a correct control on the same crawl: beat 6's "Why it is done this way" (forest surface) and beat 7's "VALLUM LABS" wordmark both render in the correct Newsreader serif, and every heading on `/dataset` ("The dataset", "Annotation schema", "Consent and provenance", "Capture specs", "Collection window", "The offer") renders correctly in Newsreader forest. The defect is localized to the home route's bone-act (beats 4 and 5) section headlines only.

What it costs: a bold sans headline dropped into an otherwise serif-and-forest system is the exact "default SaaS template" tell the type system was built to avoid (LOOK.md's own decision log calls dropping a second grotesk "old money" reasoning). Landing on the page mid-scroll, a reader sees the brand system reset twice.

Fix as a value: apply `font-family: var(--font-newsreader); font-weight: 500; color: #16281D` (the `heading`/`display-2` step already defined) to the three affected `<h2>` elements, matching the styling already correctly applied to "Why it is done this way" and the `/dataset` headings.

RESOLUTION: deferred to the morning report per arbiter iteration 2 (capacity/time-box). Listed in Not fixed.
