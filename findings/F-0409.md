---
id: F-0409
agent: critic-eyes
severity: P0
page: /
viewport: 375, 768, 1440
status: fixed
claim: Helix section and its intro copy render as flat black, zero content, all widths
---

Evidence: sampling `artifacts/{375,768,1440}/home.png` down the vertical center, every pixel from the hero's end to "the work that kills." reads an identical flat `rgb(12,11,9)` with zero variance across the full width, at all three breakpoints. `artifacts/luminance.json` bands 10 to 32 for `/` read a literal flat `11` for 23 consecutive bands (23% of page height, about 2,260px at 1440), the only run of perfectly identical values anywhere in either route's 100-band array, real photography and video never sample flat. The DOM/ARIA dump confirms real content lives in that span: "Outdoor work. At height, in the weather.", "We collect consent-cleared, action-labeled egocentric video and license it to robotics and world-model teams.", and the "HUMAN CREW" / "ROBOT TWIN" toggle that fronts the Helix. None of it, nor any Helix panel, dotted orbit line, or caption, produces a single visible pixel in any of the three full-page captures.

This is not a "dark and moody" beat reading dark, it's the absence of a render. Contrast with `artifacts/states/home-rot-*.png`: the same Helix, scrolled to and rotated by a separate, targeted script, paints correctly (glass band, panels, orbit line, caption all visible). Whatever differs between a plain scroll-through capture and the targeted rotation script is the trigger, most likely a scroll-linked mount/reveal (IntersectionObserver-gated fade-in, or a canvas/WebGL context that never receives real dimensions) that never fires when the page is captured in one pass rather than incrementally scrolled. `LOOK.md`'s own reduced-motion row for "Section entrances (bone beats)" specifies "content renders visible" as the reduced-motion fallback; if this component's reveal has no such fallback, any user with `prefers-reduced-motion: reduce` (a real, non-trivial share of visitors, and the same setting this project's own Playwright harness is noted to force) would see this exact black void on a normal scroll, not just in a capture tool.

Fix as a value: give the Helix mount and its intro-copy block an unconditional initial state of `opacity: 1` (or the reduced-motion fallback LOOK.md already specifies for other bone-beat entrances), with any scroll-linked enhancement layered on top rather than gating first paint. Verify by loading `/` fresh, setting `prefers-reduced-motion: reduce`, and scrolling straight through without pausing: the "Outdoor work..." copy and the Helix panels must be visible on arrival, not after a rotation interaction.
