---
id: F-0408
agent: critic-eyes
severity: P1
page: /
status: fixed
claim: No open-state captures exist and the 12 rotation captures show the hero, not the Helix
viewport: state
---

What I see: `artifacts/states/` contains exactly `home-focus.png` and `home-rot-00.png` through `home-rot-11.png`. There are no `home-open-*.png` files anywhere in the artifact tree. Separately, all 12 `home-rot-*.png` frames show the same composition: a large "VALLUM" wordmark and the mono tagline "physical ai data for the outdoor economy" fixed in the same position, over changing b-roll footage (roof solar install, wind turbines, a technician on a turbine blade). That wordmark and tagline are the hero's own copy (`HeroCopy.tsx`: `data-wordmark-text`, `data-hero-monoline`), and none of the eight Helix panel captions or the typed centerpiece line (confirmed present only in `home-focus.png`, which shows the actual 3D orbit ring) appear in any of the 12 rotation frames.

Why it reads wrong: my brief's interaction-state check is explicit: "check the Helix centerpiece against every home-rot-*.png; one angle passing is not passing," and "if a state capture you need does not exist, that is a P1 finding against the crawler, not a reason to skip." Both conditions apply at once here. The rotation capture set, which should be the direct evidence for the centerpiece's 12-angle contrast acceptance test in LOOK.md, instead documents a different component (the hero) at 12 moments, so the actual required check cannot be completed from artifacts alone. Open states (panel expand/detail view) have no capture at all, so "when a panel opens is the background calm, can you read what opened" cannot be answered either. I was able to partially substitute the single `home-focus.png` frame to inspect the real Helix (see the companion finding on the missing glass band), but that's one frame standing in for what should have been thirteen purpose-built captures.

The specific change: point the rotation-capture step at the Helix's own rotation control (whatever drives the 3D ring's `dampingFactor`/auto-rotate state in `HelixStage.tsx`) rather than whatever hero interaction it's currently hooked to, and add a capture step for the panel-open/detail state so both required checks have real evidence next run.
