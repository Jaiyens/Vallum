---
id: F-0606
agent: critic-voice
severity: P2
page: /
status: fixed
claim: The rig's alt text and aria-label also drop "Labs," a second bare "Vallum"
rule: FACTS.md Entity ("Always written as two words, 'Vallum Labs.' Never 'Vallum' alone")
---

**String:**

"Exploded view of the Vallum capture rig concept. A head mounted unit with a front sensor bar, housing, mainboard, side arm, batteries, storage, and head strap, separated along one explosion axis."

**Location:** `components/future-rig/callouts.ts` — `SECTION_COPY.visualLabel`. Rendered as both `aria-label` (`role="img"`) and `alt` text in `components/future-rig/rig-static.tsx`, and captured verbatim in `artifacts/1440/home.dom.txt`'s aria snapshot.

**The rule it breaks and the score:** Same Entity rule as F-0605 — "Vallum" appears without "Labs." Lower severity than the meta description because this string is assistive-tech-only and never rendered as visible copy, but it is exactly the kind of string a crawl of visible page text alone would miss, and it's an independent second occurrence of the same naming slip in a different file, which suggests the naming rule isn't being checked at string-authoring time rather than a one-off typo. Gate G5 = 0.

**The rewrite:**

"Exploded view of the Vallum Labs capture rig concept. A head mounted unit with a front sensor bar, housing, mainboard, side arm, batteries, storage, and head strap, separated along one explosion axis."
