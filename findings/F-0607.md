---
id: F-0607
agent: critic-voice
severity: P2
page: /
status: fixed
claim: Hero's opening line is an unattributed maxim, the exact pattern VOICE.md bans
rule: Banned construction 8 / register consistency (scores fine, still wrong)
---

**String:**

"This is the work humans were never meant to do."

**Location:** `src/content/hero.ts` — `HERO_COPY.beatOne`. The first line the visitor reads once the wordmark resolves; the cold open of the entire site.

**The rule it breaks and the score:** This sentence passes the mechanical rubric — short, active-reading, no banned words, no gate trips (gates pass; checks pass on a literal read). It is a finding anyway, per the instruction that a sentence sounding like the old site is a finding even when it scores fine. It reads as a floating aphorism with no procedure, mechanism, or named entity behind it: exactly the shape VOICE.md's Banned Construction 8 names as the weak set's signature move ("Scale ships an aphorism where evidence should be... A maxim claims wisdom; a procedure claims work"). It's also the one moment on the page that trades the otherwise disciplined spec register for destiny-framing ("meant to do") — a register drift against a site that is concrete and dated everywhere else, including one beat later: `HERO_COPY.beatTwo` ("Machines learn this work by watching it done. We film the people who still do it, so the machines can learn.") is exactly the procedural sentence that should be doing this job instead.

**The rewrite:** keeps the danger claim, drops the maxim, and ties forward to the gallery's own "the work that kills" framing rather than floating alone as an aphorism. No new fact, no number, no repeated fatality statistic:

"This work still kills the people who do it."
