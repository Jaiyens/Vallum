---
id: F-0417
agent: critic-eyes
severity: P2
page: /
viewport: 1440
status: wontfix
claim: Heading's second sentence "The work no one records." never appears in any render
---

`artifacts/1440/home.dom.txt` ARIA tree gives the grid heading's full accessible name as `"The work that kills. The work no one records." [level=2]`, but the TEXT extraction (rendered text) contains only "the work that kills." with no trace of the second sentence anywhere in the same file. Every screenshot reviewed (375/768/1440 full page, and both static crops of that section) shows only the first sentence. If the second sentence is meant to appear via a scroll- or time-triggered swap, no state in this crawl captures it, which mirrors the same class of failure as F-0409 (content that exists in the DOM but never gets a rendered frame in a normal pass through the page).

Fix as a value: confirm whether "The work no one records." is a live second phrase (and if so give it the same guaranteed-visible fallback recommended in F-0409) or dead copy left in the accessible name after a content edit; if the latter, remove it from the heading's accessible name so it matches what's rendered.

RESOLUTION: deferred to the morning report per arbiter iteration 2 (capacity/time-box). Listed in Not fixed.
