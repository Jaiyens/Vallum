---
id: F-0406
agent: critic-eyes
severity: P2
page: /
status: open
claim: A mono, uppercase, letter-spaced tag label sits in the hero, the banned eyebrow shape
viewport: state
---

What I see: the top-left corner of `artifacts/states/home-focus.png` shows "ROBOT TWIN" in small, all-caps, letter-spaced monospace type, in a muted sage color. The DOM confirms a matching sibling pair, "HUMAN CREW" and "ROBOT TWIN" (`artifacts/1440/home.dom.txt` lines 12 to 13), a state tag that swaps between the two labels.

Why it reads wrong: my brief lists "no mono-caps eyebrows" as one of the concrete old-money-decomposed tells to check for, and this is exactly that shape: small mono type, tracked, uppercase, functioning as a label over media. LOOK.md's data step is explicit that mono is "never a label, never an eyebrow," and this tag has no other job. It's a small element and the color itself is restrained enough not to compound the problem, but the pattern itself is the thing that was ruled out.

The specific change: if a human/robot state indicator is needed, set it at the small step (Inter 400, 14px, no letter-spacing) in sentence case, or drop the label and let the visual state (blurred vs sharp, or whatever distinguishes the twin) carry the meaning without a tracked mono caption.
