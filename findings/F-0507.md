---
id: F-0507
agent: critic-flow
severity: P1
page: /
status: wontfix
claim: Tab jumps from the last gallery button straight to the footer, skipping 5 beats
---

The path I tried: walked the keyboard tab order end to end on the live NEW state (artifacts/1440/home.dom.txt ARIA tree, cross-checked against app/layout.tsx, components/gallery/GalleryPanel.tsx, components/sections/turn.tsx, method.tsx, dataset.tsx, ethos.tsx, footer.tsx, and components/future-rig/*.tsx), the same walk that produced F-0503 last iteration.

What happened: the full list of focusable elements on the route, in DOM order, is: header nav "Vallum Labs" and "Dataset" (2), the 8 helix gallery panel buttons (agriculture through drilling rigs), then footer nav "Home" and "Dataset" (2). That is 12 tab stops total. Everything between the 8th gallery button and the footer nav, "Machines are learning this work by watching," "Consent first, camera second," the rig section's exploded-view render, "What ships with every clip" with its full annotation-schema and consent-and-provenance lists, and "The offer" itself (the pricing paragraph a keyboard-using buyer most needs to reach), is plain heading and paragraph markup with zero buttons, links, or landmarks. F-0503 flagged this exact gap last iteration and is marked fixed; the fix that shipped (F-0504's header/footer nav links) added tab stops at the very top and very bottom of the document but did not add anything in the middle, so the underlying dead zone survives this rebuild unchanged.

Where the reader stops: a keyboard-only or screen-reader user tabbing through the page presses Tab once after the gallery and lands on the footer's "Home" link, never having had a tab stop anywhere near the method, the rig, the schema, or the offer, the section that carries the pilot price and terms. They can still reach the content by reading (browse-mode arrow keys for screen readers), but there is no way to jump to or interact with any of that content via Tab, and no in-page landmark to navigate by section for AT users who rely on headings plus interactive-element jumps together.

The fix: give the back half of the page (beat 4 through beat 6) at least one real interactive element, unchanged from F-0503's original ask, e.g. a back-to-top control or an in-page jump link, so keyboard and landmark navigation has stops through the offer and ethos content, not just at the two ends of the document.

RESOLUTION: deferred to the morning report per arbiter iteration 2 (capacity/time-box). Listed in Not fixed.
