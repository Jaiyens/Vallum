---
id: F-0902
agent: warden
severity: P0
page: /
status: fixed
claim: The homepage's H1 wordmark renders "VALLUM" alone, dropping "Labs"
---

The exact string: `HERO_COPY.wordmark = "VALLUM"` in `src/content/hero.ts` line 6, rendered by `components/hero/HeroCopy.tsx` line 99 as `<h1 data-wordmark>{HERO_COPY.wordmark}</h1>`. This is the page's main heading, the first thing a screen reader announces and the first thing the eye lands on.

Not touched by tonight's diff (both lines are unchanged context), which is exactly why it survived this far uncaught. The rule does not carry a grandfather clause: "Vallum" must always be "Vallum Labs" (two words). Never "Vallum" alone. The instruction to this gate is explicit that both rendered copy and metadata get checked against this rule, with no exception for content that predates the current build wave. A pre-existing violation on the site's single most prominent piece of type is not a smaller problem than a new one, it is a bigger one.

REMOVE THE CLAIM: the h1 may not read "VALLUM" alone at any size. Either set `HERO_COPY.wordmark` to "VALLUM LABS" (or the display-cased equivalent the type system uses) and re-check the lens/overgrown-wordmark asset at `/hero/wordmark-vines.webp` for the same bare-word problem, or restructure the h1 so "Labs" renders as part of the same accessible name, not a visually separate afterthought. No builder owns `components/hero/HeroCopy.tsx` or `src/content/hero.ts`'s wordmark line this wave; this is flagged for the next iteration's charter, not fixed here.
