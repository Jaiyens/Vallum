---
id: F-0414
agent: critic-eyes
severity: P1
page: /
viewport: 1440
status: wontfix
claim: Stat-panel close button focus ring has visible rounded corners, radius banned
---

Zoomed crop of `artifacts/states/home-focus.png` (top-right "X" close control of the stat takeover, approx x:1360-1430 y:10-60) shows a clearly rounded-corner outline around the button, a true curve, not anti-aliasing. `LOOK.md`'s Banned list states: "`border-radius` above 0. The radius on this site is 0," and the focus-ring spec calls only for "2px solid `bone` with 2px offset on ink," with no radius named, consistent with the rest of the system being hard-edged. This is the only rounded element found across every screenshot reviewed; every other button, panel, and image edge in the crawl is a hard 90-degree corner.

Fix as a value: set `border-radius: 0` on the close button element (or its focus-visible outline, whichever carries the radius), matching the 0-radius rule applied everywhere else on the site.

RESOLUTION: deferred to the morning report per arbiter iteration 2 (capacity/time-box). Listed in Not fixed.
