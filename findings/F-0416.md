---
id: F-0416
agent: critic-eyes
severity: P2
page: /dataset
viewport: 1440
status: wontfix
claim: /dataset has no footer; page ends on a bare "Home" link, unlike /
---

`artifacts/1440/home.dom.txt` shows a `navigation "Footer"` landmark with `Home` and `Dataset` links plus the paragraph "Vallum Labs Inc. Cape Town, South Africa." `artifacts/1440/dataset.dom.txt` has neither: no footer landmark, no legal-entity line, the page simply ends with a plain paragraph-level "Home" link. Pixel sampling confirms the last row of `artifacts/1440/dataset.png` is plain bone (`242,238,229`), no forest bar. This is the page most likely to be read by a buyer's legal or procurement reviewer checking the entity, and it is the one route missing the entity line entirely.

Fix as a value: render the same `<footer>` component used on `/` (forest surface, "Vallum Labs Inc. Cape Town, South Africa." plus Home/Dataset nav) on `/dataset`.

RESOLUTION: deferred to the morning report per arbiter iteration 2 (capacity/time-box). Listed in Not fixed.
