---
id: F-0605
agent: critic-voice
severity: P1
page: /
status: fixed
claim: Meta and OpenGraph descriptions both call the company "Vallum," not "Vallum Labs"
rule: FACTS.md Entity ("Always written as two words, 'Vallum Labs.' Never 'Vallum' alone")
---

**String:**

"Vallum records the people who still do dangerous outdoor work and turns that footage into physical AI training data for robotics labs."

**Location:** `app/layout.tsx` — used identically as `metadata.description` and `metadata.openGraph.description` (the same string, twice).

**The rule it breaks and the score:** FACTS.md, Entity: "Always written as two words, 'Vallum Labs.' Never 'Vallum' alone." This string is the exact copy every search result snippet and every social-share preview card renders for this site, and it opens with the banned bare form, in both of the two meta tags that carry it. The page's own `<title>` gets this right ("Vallum Labs: physical AI data for the outdoor economy") one tag away; the description does not. This is not a rubric taste call, it's a direct fact-ledger naming violation on the single highest-visibility string outside the rendered page itself. Gate G5 (claim inconsistent with FACTS.md) = 0.

**The rewrite:**

"Vallum Labs records the people who still do dangerous outdoor work and turns that footage into physical AI training data for robotics labs."
