---
id: F-0415
agent: critic-eyes
severity: P1
page: /
viewport: 1440
status: wontfix
claim: Stat takeover's blur/dim backdrop stops short of viewport bottom, text bleeds through
---

In `artifacts/states/home-panel-open.png` (900px tall viewport), everything above y≈735 is correctly blurred/dimmed behind the open stat panel. Below that line, the backdrop treatment stops: the underlying grid's own captions render fully sharp and at full opacity, "year are attributed to" (a fragment of the same "Roughly 130 deaths..." stat the panel is actively typing out above it) and the complete, crisp "rooftop solar / In the US, roofing has a fatality rate of 48.7 per 100,000 workers. BLS" card, both sitting in the open black space directly under the panel's own image and copy. The effect is two versions of overlapping stat text rendered together with no blur or scrim separating them, directly under the modal that is supposed to have taken over the view. `LOOK.md`'s Recipe B for the Helix's own focus state calls for the blur to cover "the whole scene canvas... the entire background, not a region"; this takeover panel's backdrop treatment does not extend to the full viewport the same way.

Fix as a value: extend the backdrop blur/dim layer to `height: 100vh` (or `100%` of the takeover's fixed-position container) rather than a fixed or content-height box, so no unblurred page content can sit below the panel while it is open.

RESOLUTION: deferred to the morning report per arbiter iteration 2 (capacity/time-box). Listed in Not fixed.
