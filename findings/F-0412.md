---
id: F-0412
agent: critic-eyes
severity: P1
page: /
viewport: 1440
status: fixed
claim: "The rig we are building for the field" heading is missing, leaving a blank gap
---

The ARIA snapshot in `artifacts/1440/home.dom.txt` lists `heading "The rig we are building for the field" [level=2]` immediately before the paragraph "A purpose-built egocentric rig. Every component earns its place in the data." In the actual render (`artifacts/1440/home.png`, y≈6000-6450), there is no heading-styled text there at all, not low-contrast, not off-color, just uniform bone (`#F2EEE5`) for roughly 340px, then the body-sized sentence appears alone doing the job of a section headline. Every other beat 4/5 section in the same crawl has a heading directly above its lead sentence with no such gap ("Consent first, camera second" precedes its paragraph immediately; "What ships with every clip" precedes its paragraph immediately).

What it costs: this section is the only one in the bone act with no display anchor, so it reads as a stray paragraph rather than a named section, and the accessibility tree promises a heading a sighted user never sees.

Fix as a value: render the existing `<h2>The rig we are building for the field</h2>` node with the same `display-2`/forest styling as the other beat 4/5 headings (see F-0410), rather than a node with zero visible size/opacity.
