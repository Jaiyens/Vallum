---
id: R-critic-flow
round: rebuttal
iteration: 1
agent: critic-flow
---

## Disagree

- F-0006, F-0007 (agent: brief). Both assert failure-mode-shaped requirements as open P1 findings against the current build ("section must NOT eat vertical page scroll at 375", "Escape/click-outside/focus trap, focus returns"), but neither cites a captured instance of the failure actually happening on this build. My own instruction is to trace an actual path and report what happened. artifacts/states contains no touch-interaction or panel-open capture for the Helix at all, only 12 hero-rotation frames and one keyboard-focus frame, a gap F-0408 independently confirms from the visual side. Until a capture exists showing the Helix actually eating touch scroll or trapping focus wrong, these read as build-time spec risk, not confirmed defects, and shouldn't carry the same weight in the pool as findings traced against a rendered artifact.

## Amplify

- F-0013, F-0014, F-0015, F-0016 (foreman, GAP-01/02/04/06) plus F-0301 (critic-analyst). Converges directly with my own F-0504. IA.md commits /dataset and /founder to ship as P1 routes on top of a shared layout that today has zero anchor tags anywhere. "No nav" was a safe call for one route and becomes a dead end the moment a second and third route exist. Five independent reads (foreman times four, analyst, me) landed on the same structural gap. The arbiter should treat this as one blocking fix, a minimal cross-page affordance in layout.tsx, gating the /dataset and /founder ship rather than a follow-up.

- F-0011 (foreman), F-0105 (critic-buyer), F-0302 (critic-analyst). Converges with my own F-0503. Four independent traces hit the same wall: a reader who reads the exact offer price has no next step. Beyond the obvious missing address, F-0503 adds that this also removes the only same-page recovery path for keyboard users, no back-to-top, no in-page link once a keyboard user has scrolled past the gallery. That consequence survives even after F-0011 unblocks with a real contact fact, unless the fix also wires a tabbable control into the back half of the page rather than plain text.

- F-0304 (critic-analyst). Confirms independently what my charter asked me to check directly: screen two is where sites lose people. The Helix sits at beat 2, the site's single heaviest interaction (damped 3D orbit, click-to-open, focus trap), gated in front of content that answers none of the three buyer questions. Converges with my own F-0501, which found the section's actual payload, the fatality stats, never reaches a reader who doesn't interact with it at all.

- F-0008 (foreman) and F-0501 (mine), worth sequencing correctly rather than treating as the same fix. F-0008 is a fact-ledger problem, which stats are allowed on the page. F-0501 is a delivery problem, the allowed stats render only inside a click-gated takeover in all three render paths (3D helix, reduced-motion static grid, sub-1024 row), never as visible caption text. If the F-0008 fix keeps the same click-to-reveal architecture and only swaps the numbers behind it, the ledger passes and F-0501's delivery gap is still unfixed. The arbiter should confirm the F-0008 fix also relocates content into visible markup, not just corrects the figures behind the same wall.

- F-0006 (brief), on the underlying risk it names, touch swallowing page scroll on the Helix at 375. Even granting the evidentiary caveat above, this is exactly the screen-two failure mode my charter asked me to weight heaviest: not a bad first impression but an actual scroll trap on a phone. If it turns out true on build, it belongs near the top of the whole pool for the two-minute mobile reader.

## Conflicts with my read

- F-0017 (foreman, P3, "only if the night has room") vs F-0505 (mine, P2). Same defect: the framework default 404 with no way back into the site. I rated it P2 because IA.md, the document that seeds F-0017, commits to shipping /dataset and /founder as P1 the same night, which multiplies the site's broken-link surface immediately. F-0017 treats the 404 page as filler work; I think the sitemap expansion IA.md itself proposes raises the stakes past filler. Arbiter should rule whether the 404 fix ships alongside the two new routes or waits.

- F-0016 (foreman, P2, GAP-06 footer rebuild) vs F-0504 (mine, P1). F-0016 folds "links to /dataset and /founder" into a general footer-polish ticket at P2. I rated the missing cross-page affordance P1 on its own, because without it a reader who lands on /dataset first, IA.md's own stated common case, a forwarded URL, has zero way back to / or across to /founder except editing the address bar. That is not a polish gap, it is a dead end. Arbiter should decide whether the nav-affordance piece of F-0016 splits out and raises to P1 to match F-0504, or whether F-0504 folds into F-0016 at P2.

- F-0304 (critic-analyst) vs F-0501 (mine). Same diagnosis, the Helix's interaction cost is misplaced on beat 2, different fix. F-0304's fix keeps a click requirement: "make the base interaction a single tap to reveal a stat." Mine removes the click requirement entirely: "render one stat per panel as visible caption text." These cannot both ship as written. A single-tap-to-reveal still means a reader who never taps sees no stat, the exact failure mode I traced by scrolling straight through without clicking anything. Arbiter should pick one: always-visible caption, or a cheaper reveal gesture a scanning reader still has to perform eight times.

- F-0501 (mine) vs F-0702, F-0703, F-0706 (critic-civilian). Direct factual disagreement over what a reader actually encounters. All three civilian findings describe a scanning, scrolling reader reading the eight death-toll numbers directly off the page ("each one holding its own death number... as something you scroll and hover through"; "one number about dead workers, unsourced, sitting next to..."). I traced the same path and found the opposite. The TEXT rendering in home.dom.txt lines 15-31, what a sighted scrolling reader actually sees, contains only the eight one-word industry captions (farming, construction, fishing, and so on), zero numbers. Every fatality figure those three findings quote (108,000; 100,000; 860,000; one-in-a-hundred) exists only in the ARIA section as a button's aria-label, invisible to sighted users, surfaced only after a click opens the takeover. If those civilian reads were formed from the ARIA snapshot rather than from tracing what a sighted scrolling visitor sees, the reactions in F-0702, F-0703, and F-0706 describe an experience most visitors to this page never have. Arbiter should confirm which reading, visible text or accessibility tree, the civilian pass actually traced before weighting those three findings against the gallery's tone.
